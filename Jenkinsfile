
pipeline{
    agent {label "newslave"}
    stages{
        stage('Hello'){
            stesps {
                sh '''
                java --version
                '''
            }
        }
    }
}